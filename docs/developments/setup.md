# VyuKarya Development Setup

This document explains how to set up, run, and verify the VyuKarya application in a local development environment.

# 1. Prerequisites

Install the following tools before setting up the project:

- Node.js 20+
- npm 
- Git

Verify the installation:

```bash
node --version
npm --version
git --version